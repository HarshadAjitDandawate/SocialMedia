<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sidebar Navigation with Tailwind CSS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="flex bg-gray-100 font-sans">

    <div class="w-64 h-screen bg-white shadow-md">
        <div class="flex flex-col pt-5 gap-1">
            <a href="/community" class="sidebar-link flex items-center px-6 py-3 text-gray-900 hover:bg-indigo-200 hover:text-indigo-600 rounded-lg mx-2 transition-all duration-300">
                <i class="fas fa-users w-6 text-center"></i>
                <span class="ml-2">Community</span>
            </a>
            <a href="/posts" class="sidebar-link flex items-center px-6 py-3 text-gray-900 hover:bg-indigo-200 hover:text-indigo-600 rounded-lg mx-2 transition-all duration-300">
                <i class="fas fa-newspaper w-6 text-center"></i>
                <span class="ml-2">Posts</span>
            </a>
            <a href="/logout" class="sidebar-link flex items-center px-6 py-3 text-gray-900 hover:bg-indigo-200 hover:text-indigo-600 rounded-lg mx-2 transition-all duration-300">
                <i class="fas fa-sign-out-alt w-6 text-center"></i>
                <span class="ml-2">Logout</span>
            </a>
        </div>
    </div>

    <script>
        const currentPath = window.location.pathname; 
        const links = document.querySelectorAll('.sidebar-link');

        links.forEach(link => {
            link.classList.remove('bg-indigo-400', 'text-white');
            if (link.getAttribute('href') === currentPath) {
                link.classList.add('bg-indigo-500', 'text-white');
            }
        });
    </script>
</body>
</html>